import sys
from project.main import main

if __name__ == '__main__':
    # You might need to add the project directory to the python path
    # sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
    main()